package org.lizhao.validator.service;

//import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;

import org.lizhao.validator.entity.AlarmType;
import org.lizhao.validator.mapper.AlarmTypeMapper;
import org.springframework.stereotype.Service;

import java.util.ArrayList;
import java.util.Collection;
import java.util.List;

//@Service
public class AlarmTypeService
//        extends ServiceImpl<AlarmTypeMapper, AlarmType>
{

//    public  boolean saveBatch(Collection<AlarmType> list, int batchSize) {
//        int size = list.size();
//        int idxLimit = Math.min(batchSize, size);
//        int i = 1;
//        List<AlarmType> batchList = new ArrayList<>();
//        for (AlarmType element : list) {
//            batchList.add(element);
//            if (i == idxLimit) {
//                this.getBaseMapper().insertBatchSomeColumn(batchList);
//                batchList.clear();
//                idxLimit = Math.min(idxLimit + batchSize, size);
//            }
//            i++;
//        }
//        return true;
//    }


}
