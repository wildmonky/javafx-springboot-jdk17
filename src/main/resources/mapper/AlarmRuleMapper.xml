<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.lizhao.validator.spring.mapper.AlarmRuleMapper">

    <select id="selectByDevice" resultType="org.lizhao.validator.spring.entity.AlarmRule">
        -- 查询 设备 规范信息
        SELECT dsir.* FROM DEVICE_SCADA_INFO_RULE dsir
            LEFT JOIN DEVICE_SCADA_INFO_TYPE dsit ON dsit.ID = dsir.DEVICE_SCADA_INFO_TYPE_ID
            LEFT JOIN DEVICE d ON d.ID = dsit.DEVICE_ID
        WHERE d.ID = #{id};
    </select>
</mapper>